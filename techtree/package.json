{
  "name": "techtree",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bash build.sh",
    "dev": "npx esbuild src/main.ts --bundle --outfile=game.js --format=esm --watch --servedir=."
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
